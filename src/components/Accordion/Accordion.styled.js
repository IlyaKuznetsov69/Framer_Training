import styled from "styled-components";

const Root = styled.div`
  position: fixed;
  width: 300px;
  height: 100vh;
  background: gray;
  border-right: 1px solid black;
  z-index: 1;
`;

export default {
  Root
};
